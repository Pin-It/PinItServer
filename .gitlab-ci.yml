stages:
  - build
  - test
  
build:
  stage: build
  script:
    - pip3 install pipenv
    - pipenv install
    - export VENV_HOME_DIR=$(pipenv --venv)
    - source $VENV_HOME_DIR/bin/activate
    
test:
  stage: test
  script:
    - python pinit/manage.py test